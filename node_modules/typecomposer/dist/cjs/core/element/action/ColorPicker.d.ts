import { StyleOptional, Component } from "../..";
export declare class ColorPicker extends Component {
    private _input;
    constructor(optional?: StyleOptional & {
        value?: string;
    });
    get value(): string;
    set value(value: string);
}
