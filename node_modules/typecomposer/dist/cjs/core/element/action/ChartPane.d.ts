import { Component, type StyleOptional } from "../..";
import { Chart, ChartConfiguration } from "chart.js/auto";
/**
 * ChartElement
 * https://www.chartjs.org/docs/latest/charts
 */
export declare class ChartElement extends Component {
    private _canvas;
    private _chart;
    private ctx;
    constructor(optional?: StyleOptional & {
        type?: ChartConfiguration["type"];
        data?: ChartConfiguration["data"];
        options?: ChartConfiguration["options"];
    });
    resize(): void;
    onInit(): void;
    update(config?: ChartConfiguration): void;
    set type(type: ChartConfiguration["type"]);
    get type(): ChartConfiguration["type"];
    get configuration(): ChartConfiguration;
    set configuration(config: ChartConfiguration);
    get datasets(): ChartConfiguration["data"]["datasets"];
    set datasets(datasets: ChartConfiguration["data"]["datasets"]);
    get chart(): Chart;
    unmount(): void;
    static ChartOptionsEmpty(data?: ChartConfiguration): ChartConfiguration;
    static generateUniqueColors(qtd: number): string[];
}
