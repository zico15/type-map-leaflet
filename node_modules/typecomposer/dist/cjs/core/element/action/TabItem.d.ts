import { Component, IconElement } from "..";
export declare class TabItem extends Component {
    title: string;
    private __closeable;
    private titleElement;
    private displayContent;
    private _content;
    private closeIcon;
    constructor(title: string, content: HTMLElement, icon?: IconElement | null, closeable?: boolean);
    get closeable(): boolean;
    set closeable(value: boolean);
    select(): void;
    unselect(): void;
    get content(): HTMLElement;
}
