import { Component, StyleOptional, NavElement, Variant } from "../..";
export interface DataMenuBarElement extends StyleOptional {
    title?: string;
    variant?: Variant;
    logo?: HTMLElement | string;
}
export declare class MenuBarElement extends Component {
    private _nav;
    private _ul;
    private checkbox;
    private checkboxLabel;
    private _logo;
    constructor(optional?: DataMenuBarElement);
    get nav(): NavElement;
    get logo(): HTMLElement | undefined;
    set logo(value: HTMLElement | string);
    addMenu(menu: HTMLElement | string, isLogo?: boolean, optional?: StyleOptional): void;
}
