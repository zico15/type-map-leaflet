import { Component } from "../element";
/**
 * to remove the alert just add the underline (!) to the variable name
 * ex: name!: T;
 */
export declare function RefComponent(optional?: {
    id?: string;
} | string): any;
export declare class VFor {
    component: Component;
    elementBase: string;
    key: string;
    vfor: RegExpMatchArray;
    private orden;
    private parent;
    private static id;
    constructor(component: Component, elementBase: string, key: string, vfor: RegExpMatchArray);
    init(): void;
    update(): void;
    private setData;
    static creadList(component: Component, html: string): void;
}
