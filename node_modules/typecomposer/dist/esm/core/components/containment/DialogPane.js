import { CardElement, Component } from "../..";
export class DialogPane extends Component {
    _content = new CardElement({
        className: "content",
        width: "500px",
        height: "300px",
    });
    constructor(optional) {
        super(optional);
        if (optional?.display == undefined)
            this.style.display = "none";
        this.append(this._content);
        this.addEventListener("click", (e) => {
            this.dismiss();
            e.stopPropagation();
        });
        this._content.addEventListener("click", (e) => {
            e.stopPropagation();
        });
    }
    onInit() {
        console.log("DialogElement");
    }
    get content() {
        return this._content;
    }
    show() {
        this.style.display = "block";
    }
    dismiss() {
        this.style.display = "none";
    }
}
customElements.define("dialog-pane", DialogPane);
//# sourceMappingURL=DialogPane.js.map