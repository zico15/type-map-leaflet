import { ILayout, StyleOptional, ref } from "../..";
export declare class GridElement extends ILayout {
    constructor(optional?: StyleOptional);
    set columns(value: string | ref<string>);
    set rows(value: string | ref<string>);
}
