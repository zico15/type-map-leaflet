export function InheritClasses(...bases) {
    // https://stackoverflow.com/a/61860802
    class Bases {
        constructor(...args) {
            bases.forEach((base, i) => Object.assign(this, new base(...args[i])));
        }
    }
    bases.forEach((base) => {
        Object.getOwnPropertyNames(base.prototype)
            .filter((prop) => prop != "constructor")
            .forEach((prop) => {
            console.log("prop: ", prop);
            // Bases.prototype[prop] = base.prototype[prop];
        });
    });
    return Bases;
}
//# sourceMappingURL=InheritClasses.js.map