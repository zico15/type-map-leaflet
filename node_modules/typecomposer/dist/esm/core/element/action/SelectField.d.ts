import { DropDown, StyleOptional } from "../..";
import { SelectionType } from "./DropDown";
export declare class SelectField<T> extends DropDown<T> {
    constructor(optional?: StyleOptional & {
        placeHolder?: string;
        noContent?: string;
        options?: T[];
        defaultOption?: string;
        selectionType?: SelectionType;
    });
}
