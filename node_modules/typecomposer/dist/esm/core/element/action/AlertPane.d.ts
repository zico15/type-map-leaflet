import { Component, StyleOptional } from "../..";
export type AlertType = "success" | "info" | "warning" | "danger";
export declare class AlertPane extends Component {
    private static _div;
    constructor(options?: StyleOptional & {
        title?: string;
        message?: string;
        type: AlertType;
        timeout?: number;
    });
    static success(title: string, message: string, options?: StyleOptional & {
        timeout?: number;
    }): AlertPane;
    static info(title: string, message: string, options?: StyleOptional & {
        timeout?: number;
    }): AlertPane;
    static warning(title: string, message: string, options?: StyleOptional & {
        timeout?: number;
    }): AlertPane;
    static danger(title: string, message: string, options?: StyleOptional & {
        timeout?: number;
    }): AlertPane;
}
